<Window x:Class="HSIApp.Windows.CubeInfoWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Cube Information"
        Width="600"
        Height="520"
        WindowStartupLocation="CenterOwner">

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="{Binding DisplayName}"
                   FontSize="18"
                   FontWeight="Bold"/>

        <TextBlock Grid.Row="1"
                   Margin="0,10,0,0"
                   TextWrapping="Wrap"
                   Text="{Binding SourcePath}"/>

        <StackPanel Grid.Row="2"
                    Margin="0,14,0,0">

            <TextBlock Text="{Binding ShapeDescription}"/>
            <TextBlock Margin="0,4,0,0"
                       Text="{Binding WavelengthDescription}"/>

        </StackPanel>

        <TextBlock Grid.Row="3"
                   Margin="0,18,0,6"
                   FontWeight="Bold"
                   Text="Header metadata"/>

        <ListView Grid.Row="4"
                  ItemsSource="{Binding HeaderValues}">

            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Field"
                                    Width="190"
                                    DisplayMemberBinding="{Binding Key}"/>
                    <GridViewColumn Header="Value"
                                    Width="340"
                                    DisplayMemberBinding="{Binding Value}"/>
                </GridView>
            </ListView.View>
            
        </ListView>

    </Grid>
</Window>
